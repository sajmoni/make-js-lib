{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "refresh": "yarn cache clean && yarn install --force --no-lockfile",
    "test-all-use-cases": "./test-all-use-cases.sh"
  },
  "dependencies": {
    "make-js-lib": "file:../make-js-lib.tgz"
  }
}
