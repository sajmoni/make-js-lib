{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "node .",
    "refresh": "yarn cache clean && yarn install --force --no-lockfile",
    "test": "ava"
  },
  "dependencies": {
    "ava": "3.5.0",
    "test-lib": "file:../test-lib.tgz"
  }
}
