{
  "name": "example",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "refresh": "yarn cache clean && rm -rf node_modules && rm -f yarn.lock && yarn",
    "test": "ava"
  },
  "dependencies": {
    "ava": "^2.4.0",
    "lodash": "^4.17.15",
    "name-of-your-project": "file:../"
  }
}
